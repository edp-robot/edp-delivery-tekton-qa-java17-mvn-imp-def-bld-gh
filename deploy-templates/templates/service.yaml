apiVersion: v1
kind: Service
metadata:
  name: {{ include "java17-mvn-imp-def-bld-gh.fullname" . }}
  labels:
    {{- include "java17-mvn-imp-def-bld-gh.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "java17-mvn-imp-def-bld-gh.selectorLabels" . | nindent 4 }}
